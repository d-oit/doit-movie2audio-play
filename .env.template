# Hugging Face Token for VAD model
HUGGING_FACE_TOKEN=""  # Required for pyannote.audio voice activity detection

# TTS Configuration
TTS_MODEL_PATH="tts_models/de/thorsten/vits"  # Path to Coqui-AI TTS model
TTS_CONFIG_PATH=""  # Optional: Path to custom TTS config
TTS_VOICE_ID="tts_models/de/thorsten/vits"  # Voice ID for synthesis (German)

# Audio Processing
BACKGROUND_VOLUME_REDUCTION_DB=-15.0  # Volume reduction for original audio during narration (dB)
NARRATION_VOLUME_ADJUST_DB=0.0  # Volume adjustment for narration (dB)
AUDIO_BITRATE="192k"  # MP3 output bitrate

# Output Paths
OUTPUT_DIR="output"  # Directory for final output files
TEMP_DIR="temp"  # Directory for temporary files

# Logging
LOG_LEVEL="INFO"  # DEBUG, INFO, WARNING, ERROR
LOG_FILE="output/process_movie_audio.log"  # Path to log file

# Optional: BLIP Model Settings (defaults to Salesforce/blip-image-captioning-large)
BLIP_MODEL_NAME="Salesforce/blip-image-captioning-large"  # Model for scene description
